info:
  name: get metric by machine ID
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/metrics/machine-1
  headers:
    - name: Authorization
      value: Bearer {{process.env.API_TOKEN}}
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
